# mailbourne

A liveable mail server and library — single binary, built-in inspector for
DNS, DKIM, and deliverability.

This PyPI package is a name reservation; the engine is still brewing. It will
ship the `mailbourne` binary so you can `uvx mailbourne inspect --domain
example.com` with zero install.

Follow along: <https://github.com/mailbourne/mailbourne>
